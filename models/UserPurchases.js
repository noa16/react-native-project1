class userPurchases {
  constructor(username, activityName, location) {
    (this.username = username), (this.activityName = activityName), (this.location = location);
  }
}

export default userPurchases;