class ActivitySearch {
  constructor(title, location, age, name) {
    (this.location = location),
      (this.title = title),
      (this.age = age),
      (this.name = name);
  }
}

export default ActivitySearch;
